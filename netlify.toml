# SPA fallback: serve index.html for client-side routes so deep-links and
# refreshes on paths like /players/:id resolve to the app instead of a 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
