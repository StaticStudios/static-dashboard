VITE_API_BASE_URL=http://localhost:8080
# Clerk publishable key. Leave empty for local dev (the app runs unauthenticated and attaches no
# token; the dev backend runs with app.security.enabled=false). Set it to enable real auth.
VITE_CLERK_PUBLISHABLE_KEY=
